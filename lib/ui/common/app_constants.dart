import 'package:flutter/material.dart';
import 'package:flutter_screenutil/flutter_screenutil.dart';

const double kGlobalPadding = 14.0;

final kGlobalHorizontalPadding = const EdgeInsets.symmetric(
  horizontal: kGlobalPadding,
).r;

final kEventDetailsHorPadding = const EdgeInsets.symmetric(
  horizontal: 20,
).r;

final kGlobalBorderRadius = const BorderRadius.all(Radius.circular(20)).r;

const String kPlaceHolerImageAsset = 'assets/tec-conference.jpg';
